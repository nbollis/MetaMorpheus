<Window x:Class="MetaMorpheusGUI.FragmentTypeDetectionTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MetaMorpheusGUI"
        xmlns:guiFunctions="clr-namespace:GuiFunctions;assembly=GuiFunctions"
        mc:Ignorable="d"
        Title="FragmentTypeDetectionTaskWindow" Width="750" Height="725" d:DesignHeight="1500"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:BooleanInverter x:Key="InvertedBooleanConverter" />

        <!-- used in Quantifications -->
        <BooleanToVisibilityConverter x:Key="bool2VisConverter" />
        <local:BooleanOrConverter x:Key="boolOrConverter"/>
        <local:BooleanOrToVisibilityConverter x:Key="boolOr2VisConverter"/>

        <!-- Style GroupBoxes -->
        <Style TargetType="GroupBox">
            <Setter Property="BorderThickness" Value="3 1 1 1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="DarkSlateGray"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Black" FontWeight="Bold"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style TextBoxes -->
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="3 0 0 0"/>
        </Style>
        <Style TargetType="local:DoubleTextBoxControl">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="3 0 0 0"/>
        </Style>
    </Window.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <DockPanel>

                <!-- Task Name Input -->
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="5,0,0,0">
                    <Label Content="Enter Task Name:" />
                    <TextBox x:Name="OutputFileNameTextBox" Width="150">
                        <TextBox.Style>
                            <Style TargetType="TextBox" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                <Style.Resources>
                                    <VisualBrush x:Key="CueBannerBrush" AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                        <VisualBrush.Visual>
                                            <Label Content="FragmentDetectionTask" Foreground="Gray" Padding="5 0 0 0"/>
                                        </VisualBrush.Visual>
                                    </VisualBrush>
                                </Style.Resources>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="{x:Static sys:String.Empty}">
                                        <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                    </Trigger>
                                    <Trigger Property="Text" Value="{x:Null}">
                                        <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                    </Trigger>
                                    <Trigger Property="IsKeyboardFocused" Value="True">
                                        <Setter Property="Background" Value="White" />
                                        <Setter Property="Padding" Value="5 0 0 0"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    </Trigger>
                                    <Trigger Property="IsKeyboardFocused" Value="False">
                                        <Setter Property="Padding" Value="5 0 0 0"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </StackPanel>


                <!-- BASIC SETTINGS -->
                <StackPanel DockPanel.Dock="Top" Margin="10">

                    <!-- Search Parameters -->
                    <GroupBox Header="Search Parameters" DockPanel.Dock="Top" >
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="1*"/>
                                    <RowDefinition Height="1*"/>
                                    <RowDefinition Height="1*"/>
                                    <RowDefinition Height="1*"/>
                                    <RowDefinition Height="1*"/>
                                    <RowDefinition Height="1*"/>
                                </Grid.RowDefinitions>

                                <!-- Precursor Mass Tolerance -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="1" Grid.Row="0">
                                    <Label Content="Precursor Mass Tolerance:" Width="150" />
                                    <local:DoubleTextBoxControl x:Name="PrecursorMassToleranceTextBox" HorizontalAlignment="Left" TextWrapping="Wrap" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                        <TextBox.ToolTip>
                                            <TextBlock>
                                            Accepted tolerance between experimental and theoretical MS1 peaks.
                                            <LineBreak />
                                            This is a file specific parameter; calibration of spectra files will overwrite this value with the suggested tolerance(s).
                                            </TextBlock>
                                        </TextBox.ToolTip>
                                    </local:DoubleTextBoxControl>
                                    <ComboBox x:Name="PrecursorMassToleranceComboBox" HorizontalAlignment="Left" />
                                </StackPanel>

                                <!-- Product Mass Tolerance -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="1" Grid.Row="1">
                                    <Label Content="Product Mass Tolerance:" Width="150" />
                                    <local:DoubleTextBoxControl x:Name="ProductMassToleranceTextBox" HorizontalAlignment="Left" TextWrapping="Wrap" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                        <TextBox.ToolTip>
                                            <TextBlock>
                                            Accepted tolerance between experimental and theoretical MS2 peaks.
                                            <LineBreak />
                                            This is a file specific parameter; calibration of spectra files will overwrite this value with the suggested tolerance(s).
                                            </TextBlock>
                                        </TextBox.ToolTip>
                                    </local:DoubleTextBoxControl>
                                    <ComboBox x:Name="ProductMassToleranceComboBox" HorizontalAlignment="Left" />
                                </StackPanel>

                                <!-- Dissociation type -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="1" Grid.Row="2">

                                </StackPanel>

                                <!-- Max missed cleavages -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="0" Grid.Row="1">
                                    <Label x:Name="maxMissedCleavagesLabel" Content="Max Missed Cleavages:" Width="150" />
                                    <local:IntegerTexBoxControl x:Name="MissedCleavagesTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500" >
                                        <TextBox.ToolTip>
                                            <TextBlock>
                                    Maximum number of missed proteolytic cleavages allowed.
                                    <LineBreak />
                                    Semi-specific cleavages do not count toward this.
                                            </TextBlock>
                                        </TextBox.ToolTip>
                                    </local:IntegerTexBoxControl>
                                </StackPanel>

                                <!-- Protease -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="0" Grid.Row="0">
                                    <Label x:Name="proteaseLabel" Width="150" >
                                        <Label.Content>
                                            <Binding Converter="{StaticResource ProteinRnaTerminologyConverter}" ConverterParameter="Protease:"/>
                                        </Label.Content>
                                    </Label>
                                    <ComboBox x:Name="ProteaseComboBox" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500" >
                                        <ToolTipService.ToolTip>
                                            <ToolTip>
                                                <ToolTip.Content>
                                                    <Binding Converter="{StaticResource ProteinRnaTerminologyConverter}"
                                                                          ConverterParameter="Protease used for digestion.&#x0a;* Proteases can be added/modified by the user at &quot;Data -> Open folder with mods/data files -> ProteolyticDigestion -> proteases.tsv&quot;&#x0a;* For semi-specific digestions, select &quot;Semi-Specific Search&quot; in &quot;Search Mode&quot;."/>
                                                </ToolTip.Content>
                                            </ToolTip>
                                        </ToolTipService.ToolTip>
                                    </ComboBox>
                                </StackPanel>

                                <!-- Initiator methionine behavior -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="1" Grid.Row="3" Visibility="{Binding  Converter={StaticResource CollapseOnRnaModeConverter}}">
                                    <Label x:Name="initiatorMethionineLabel" Content="Initiator Methionine:" Width="150" />
                                    <ComboBox x:Name="InitiatorMethionineBehaviorComboBox" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                        <ComboBox.ToolTip>
                                            <TextBlock>
                                                    Assumed cleavage of the initiator Methionine present at the beginning of most proteins (start codon).
                                            </TextBlock>
                                        </ComboBox.ToolTip>
                                    </ComboBox>
                                </StackPanel>

                                <!-- Separation type -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="1" Grid.Row="4">
                                    <Label x:Name="separationTypeLabel" Content="Separation Type:" Width="150" />
                                    <ComboBox x:Name="SeparationTypeComboBox" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                        <ComboBox.ToolTip>
                                            <TextBlock>
                                                    Select the separation type that was use prior to MS analysis. Options are HPLC and CZE.
                                            </TextBlock>
                                        </ComboBox.ToolTip>
                                    </ComboBox>
                                </StackPanel>


                                <!-- Min peptide lengths -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="0" Grid.Row="3">
                                    <Label x:Name="lblMinPeptideLength" Width="150" >
                                        <Label.Content>
                                            <Binding Converter="{StaticResource ProteinRnaTerminologyConverter}" 
                                                     ConverterParameter="Min Peptide Length:"/>
                                        </Label.Content>
                                    </Label>
                                    <local:IntegerTexBoxControl x:Name="MinPeptideLengthTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                        <TextBox.ToolTip>
                                            <ToolTip>
                                                <ToolTip.Content>
                                                    <Binding Converter="{StaticResource ProteinRnaTerminologyConverter}" 
                                                             ConverterParameter="The minimum allowed peptide length. Must be a positive, non-zero integer."/>
                                                </ToolTip.Content>
                                            </ToolTip>
                                        </TextBox.ToolTip>
                                    </local:IntegerTexBoxControl>
                                </StackPanel>

                                <!-- Max peptide lengths -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="0" Grid.Row="4">
                                    <Label x:Name="lblMaxPeptideLength" Width="150" >
                                        <Label.Content>
                                            <Binding Converter="{StaticResource ProteinRnaTerminologyConverter}" 
                                                     ConverterParameter="Max Peptide Length:"/>
                                        </Label.Content>
                                    </Label>
                                    <local:IntegerTexBoxControl x:Name="MaxPeptideLengthTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                        <TextBox.ToolTip>
                                            <ToolTip>
                                                <ToolTip.Content>
                                                    <Binding Converter="{StaticResource ProteinRnaTerminologyConverter}" 
                                                             ConverterParameter="The maximum allowed peptide length. Leave empty for no limitation."/>
                                                </ToolTip.Content>
                                            </ToolTip>
                                        </TextBox.ToolTip>
                                    </local:IntegerTexBoxControl>
                                </StackPanel>

                                <!-- Max mods per peptide -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="0" Grid.Row="2">
                                    <Label x:Name="lbMaxModNum" Width="150" >
                                        <Label.Content>
                                            <Binding Converter="{StaticResource ProteinRnaTerminologyConverter}" 
                                                     ConverterParameter="Max Mods Per Peptide:"/>
                                        </Label.Content>
                                    </Label>
                                    <local:IntegerTexBoxControl x:Name="MaxModNumTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                        <TextBox.ToolTip>
                                            <ToolTip>
                                                <ToolTip.Content>
                                                    <Binding Converter="{StaticResource ProteinRnaTerminologyConverter}" 
                                                             ConverterParameter="The maximum number of modifications allowed on a single isoform."/>
                                                </ToolTip.Content>
                                            </ToolTip>
                                        </TextBox.ToolTip>
                                    </local:IntegerTexBoxControl>
                                </StackPanel>

                                <!-- Q-Value Filter -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="0" Grid.Row="6">
                                    <Label x:Name="QValueThreshold" Content="Q-Value Threshold:" Width="150" />
                                    <local:DoubleTextBoxControl x:Name="QValueThresholdTextBox"  Width="45" 
                                             ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500"
                                             IsEnabled="{Binding ElementName=PepQValueThresholdCheckbox, Path=IsChecked, Converter={StaticResource InvertedBooleanConverter}}">
                                        <TextBox.ToolTip>
                                            <TextBlock>
                                                   The maximum q-value of peptides used for quantification and spectral library generation. Protein group construction is not affected by this threshold.
                                                <LineBreak />
                                                    If the q-value threshold is set, PEP q-value filtering will not be performed.
                                            </TextBlock>
                                        </TextBox.ToolTip>
                                    </local:DoubleTextBoxControl>
                                </StackPanel>

                                <!-- PEP Q-Value Filter -->
                                <StackPanel Orientation="Horizontal" Margin="5 5 5 0" Grid.Column="1" Grid.Row="6">
                                    <!-- pep 1-value button -->
                                    <CheckBox x:Name="PepQValueThresholdCheckbox" Content="PEP Q-Value Threshold:" 
                                                 IsChecked="False" Margin="5,5,5,0" />
                                    <local:DoubleTextBoxControl x:Name="PepQValueThresholdTextBox"
                                             IsEnabled="{Binding IsChecked, ElementName=PepQValueThresholdCheckbox}" Width="45"
                                             Height="23" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                        <TextBox.ToolTip>
                                            <TextBlock>
                                                    The maximum q-value of peptides used for quantification and spectral library generation. Protein group construction is not affected by this threshold.
                                                    <LineBreak />
                                                    If the PEP q-value threshold is set, traditional q-value filtering will not be performed.
                                            </TextBlock>
                                        </TextBox.ToolTip>
                                    </local:DoubleTextBoxControl>
                                </StackPanel>

                            </Grid>
                        </StackPanel>
                    </GroupBox>

                    <!-- Modifications (Fixed/Variable/GPTMD) -->
                    <GroupBox Header="Modifications" DockPanel.Dock="Top">
                        <StackPanel Height="145">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="1*" />

                                </Grid.ColumnDefinitions>
                                <StackPanel Margin="5 0 5 0">
                                    <Label Content="Fixed:" />
                                    <TreeView x:Name="FixedModsTreeView" ItemsSource="{Binding}" DataContext="{x:Type guiFunctions:ModTypeForTreeViewModel}" Height="118">
                                        <TreeView.Resources>
                                            <HierarchicalDataTemplate DataType="{x:Type guiFunctions:ModTypeForTreeViewModel}" ItemsSource="{Binding Children}">
                                                <StackPanel Orientation="Horizontal" Background="{Binding Background}">
                                                    <CheckBox IsChecked="{Binding Use}" />
                                                    <TextBlock Text="{Binding DisplayName}" />
                                                </StackPanel>
                                            </HierarchicalDataTemplate>
                                            <DataTemplate DataType="{x:Type guiFunctions:ModForTreeViewModel}">
                                                <StackPanel Orientation="Horizontal" Background="{Binding Background}" ToolTip="{Binding ToolTipStuff}">
                                                    <CheckBox IsChecked="{Binding Use}" />
                                                    <TextBlock Text="{Binding DisplayName}" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </TreeView.Resources>
                                    </TreeView>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="5 0 3 0">
                                    <Label Content="Variable:"  ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                        <Label.ToolTip>
                                            <TextBlock>
                                                    Only use this for highly prevelant modifications (>10% of peptides).
                                                    <LineBreak />
                                                    All PTMs that are annotated in the database (e.g. with G-PTM-D) will be automatically searched even without these boxes checked.
                                                    <LineBreak />
                                                    Do not check annotated PTMs here.
                                            </TextBlock>
                                        </Label.ToolTip>
                                    </Label>
                                    <TreeView x:Name="VariableModsTreeView" ItemsSource="{Binding}" DataContext="{x:Type guiFunctions:ModTypeForTreeViewModel}" Height="118">
                                        <TreeView.Resources>
                                            <HierarchicalDataTemplate DataType="{x:Type guiFunctions:ModTypeForTreeViewModel}" ItemsSource="{Binding Children}">
                                                <StackPanel Orientation="Horizontal" Background="{Binding Background}">
                                                    <CheckBox IsChecked="{Binding Use}" />
                                                    <TextBlock Text="{Binding DisplayName}" />
                                                </StackPanel>
                                            </HierarchicalDataTemplate>
                                            <DataTemplate DataType="{x:Type guiFunctions:ModForTreeViewModel}">
                                                <StackPanel Orientation="Horizontal" Background="{Binding Background}" ToolTip="{Binding ToolTipStuff}">
                                                    <CheckBox IsChecked="{Binding Use}" />
                                                    <TextBlock Text="{Binding DisplayName}" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </TreeView.Resources>
                                    </TreeView>
                                </StackPanel>
                                <TextBox x:Name="SearchFixMod" TextChanged="TextChanged_Fixed" HorizontalAlignment="Left" Margin="82,4,0,410" TextWrapping="Wrap" Width="140">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                            <Style.Resources>
                                                <VisualBrush x:Key="CueBannerBrush" AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <Label Content="Search..." Foreground="Gray" />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Style.Resources>
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="{x:Static sys:String.Empty}">
                                                    <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                                </Trigger>
                                                <Trigger Property="Text" Value="{x:Null}">
                                                    <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsKeyboardFocused" Value="True">
                                                    <Setter Property="Background" Value="White" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                                <TextBox x:Name="SearchVarMod" TextChanged="TextChanged_Var" Grid.Column="1" HorizontalAlignment="Left" Margin="82,4,0,410" TextWrapping="Wrap" Width="140">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                            <Style.Resources>
                                                <VisualBrush x:Key="CueBannerBrush" AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <Label Content="Search..." Foreground="Gray" />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Style.Resources>
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="{x:Static sys:String.Empty}">
                                                    <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                                </Trigger>
                                                <Trigger Property="Text" Value="{x:Null}">
                                                    <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsKeyboardFocused" Value="True">
                                                    <Setter Property="Background" Value="White" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                            </Grid>
                        </StackPanel>
                    </GroupBox>

                    <!-- Output Options -->
                    <GroupBox Header="Output Options" DockPanel.Dock="Top" ScrollViewer.CanContentScroll="True">

                    </GroupBox>
                </StackPanel>
                <!-- END BASIC SETTINGS -->

                <!-- ADVANCED SETTINGS -->
                <Expander Header="Advanced Options" DockPanel.Dock="Top" Margin="10,0,10,10" >
                    <StackPanel>
                        <GroupBox Header="File Loading Parameters" DockPanel.Dock="Top">
                            <Expander x:Name="FileLoadExpander">
                                <Expander.Style>
                                    <Style TargetType="Expander">
                                        <Style.Triggers>
                                            <Trigger Property="IsExpanded" Value="True">
                                                <Setter Property="Header" Value="Hide" />
                                            </Trigger>
                                            <Trigger Property="IsExpanded" Value="False">
                                                <Setter Property="Header" Value="Some file loading properties..." />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Expander.Style>
                                <StackPanel>
                                    <local:HostDeconParamControl x:Name="DeisotopingControl" DataContext="{Binding}"/>
                                    <GroupBox Header="Peak Trimming">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="10,5,5,5">
                                                <CheckBox x:Name="TrimMs1" Content="Trim MS1 Peaks" IsChecked="True" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <ToolTipService.ToolTip>
                                                        <ToolTip Content="Remove low-intensity MS1 peaks using the below settings" />
                                                    </ToolTipService.ToolTip>
                                                </CheckBox>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="10,5,5,5">
                                                <CheckBox x:Name="TrimMsMs" Content="Trim MS2 Peaks" IsChecked="True" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <ToolTipService.ToolTip>
                                                        <ToolTip Content="Remove low-intensity MS2 peaks using the below settings" />
                                                    </ToolTipService.ToolTip>
                                                </CheckBox>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <Label Content="Top N peaks per m/z window:" Width="193" />
                                                <local:IntegerTexBoxControl x:Name="NumberOfPeaksToKeepPerWindowTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <ToolTipService.ToolTip>
                                                        <ToolTip Content="Use the N most intense peaks from the spectrum and remove all other peaks" />
                                                    </ToolTipService.ToolTip>
                                                </local:IntegerTexBoxControl>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <Label Content="Minimum intensity ratio:" Width="193" />
                                                <local:DoubleTextBoxControl x:Name="MinimumAllowedIntensityRatioToBasePeakTexBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <ToolTipService.ToolTip>
                                                        <ToolTip Content="Peaks must be this intense compared to the base peak" />
                                                    </ToolTipService.ToolTip>
                                                </local:DoubleTextBoxControl>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <Label Content="Nominal window width Thomsons:" />
                                                <local:DoubleTextBoxControl x:Name="WindowWidthThomsonsTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <ToolTipService.ToolTip>
                                                        <ToolTip Content="Divide the MS2 spectrum into windows N-Thomsons wide before filtering peaks" />
                                                    </ToolTipService.ToolTip>
                                                </local:DoubleTextBoxControl>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <Label Content="Number of windows:" Width="193" />
                                                <local:IntegerTexBoxControl x:Name="NumberOfWindowsTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <ToolTipService.ToolTip>
                                                        <ToolTip Content="Divide the MS2 spectrum into an integer number of evenly sized windows before filtering peaks" />
                                                    </ToolTipService.ToolTip>
                                                </local:IntegerTexBoxControl>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="10,5,5,5">
                                                <CheckBox x:Name="NormalizePeaksInWindowCheckBox" Content="Normalize peaks in each window" IsChecked="False" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <CheckBox.ToolTip>
                                                        <TextBlock>
                                                    Normalize the peaks in each window so that the maximum intensity is the same accross all windows.
                                                        </TextBlock>
                                                    </CheckBox.ToolTip>
                                                </CheckBox>
                                            </StackPanel>
                                        </StackPanel>
                                    </GroupBox>
                                </StackPanel>
                            </Expander>
                        </GroupBox>

                        <GroupBox Header="Search Parameters" DockPanel.Dock="Top">
                            <Expander x:Name="SearchModeExpander" >
                                <Expander.Style>
                                    <Style TargetType="Expander">
                                        <Setter Property="Header" Value="Some search properties..." />
                                        <Style.Triggers>
                                            <Trigger Property="IsExpanded" Value="True">
                                                <Setter Property="Header" Value="Hide" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Expander.Style>
                                <StackPanel>
                                    <GroupBox Header="In-Silico Digestion Parameters">
                                        <StackPanel Margin="2">
                                            <CheckBox x:Name="CheckBoxTarget" Margin="5 1 1 1" DockPanel.Dock="Top" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                <CheckBox.Content>
                                                    <Binding Converter="{StaticResource ProteinRnaTerminologyConverter}"
                                                             ConverterParameter="Generate target proteins"/>
                                                </CheckBox.Content>
                                                <CheckBox.ToolTip>
                                                    <TextBlock>
                                                    Search the forward sequences present in the database. Uncheck this to allow for separate target/decoy searching.
                                                    </TextBlock>
                                                </CheckBox.ToolTip>
                                            </CheckBox>
                                            <CheckBox x:Name="CheckBoxDecoy" Margin="5 1 1 1" DockPanel.Dock="Top" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                <CheckBox.Content>
                                                    <Binding Converter="{StaticResource ProteinRnaTerminologyConverter}"
                                                             ConverterParameter="Generate decoy proteins"/>
                                                </CheckBox.Content>
                                                <CheckBox.ToolTip>
                                                    <TextBlock>
                                                    Search decoy sequences by transformation of the database. Uncheck this to allow for separate target/decoy searching.
                                                    </TextBlock>
                                                </CheckBox.ToolTip>
                                            </CheckBox>
                                            <RadioButton x:Name="RadioButtonReverseDecoy" Content="Generate reversed decoys" GroupName="DecoyType" Margin="5 1 1 1" DockPanel.Dock="Top" IsEnabled="{Binding IsChecked, ElementName=CheckBoxDecoy}" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500"
                                                         Visibility="{Binding  Converter={StaticResource CollapseOnRnaModeConverter}}">
                                                <RadioButton.ToolTip>
                                                    <TextBlock>
                                                    Generate decoy sequences by reversing the original sequences.
                                                   <LineBreak />
                                                    Methionine is fixed at the N-terminus and the protein is reversed before in silico digestion.
                                                    </TextBlock>
                                                </RadioButton.ToolTip>
                                            </RadioButton>
                                            <RadioButton x:Name="RadioButtonSlideDecoy" Content="Generate slided decoys" GroupName="DecoyType" Margin="5 1 1 1" DockPanel.Dock="Top" IsEnabled="{Binding IsChecked, ElementName=CheckBoxDecoy}" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500"
                                                         Visibility="{Binding  Converter={StaticResource CollapseOnRnaModeConverter}}">
                                                <RadioButton.ToolTip>
                                                    <TextBlock>
                                                    Generate decoy sequences via a non-random shuffle of the original sequences.
                                                   <LineBreak />
                                                    Methionine is fixed at the N-terminus.
                                                    </TextBlock>
                                                </RadioButton.ToolTip>
                                            </RadioButton>
                                            <StackPanel Orientation="Horizontal" Margin="2">
                                                <Label x:Name="maxModificationIsoformsLabel" Content="Max Modification Isoforms:" Width="250" />
                                                <local:IntegerTexBoxControl x:Name="maxModificationIsoformsTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <TextBox.ToolTip>
                                                        <TextBlock>
                                                        The maximum number of isoforms (sequences with modifications) allowed from a given unmodified "base-sequence".
                                                        <LineBreak />
                                                        This prevents highly ambiguous regions from greatly expanding the search space.
                                                        </TextBlock>
                                                    </TextBox.ToolTip>
                                                </local:IntegerTexBoxControl>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="2">
                                                <Label x:Name="LbMinVariantDepth" Content="Min Read Depth for Variants:" Width="250" />
                                                <local:IntegerTexBoxControl x:Name="MinVariantDepthTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <TextBox.ToolTip>
                                                        <TextBlock>
                                                    The minimum number of reads to consider a nucleic acid sequence variant.
                                                        </TextBlock>
                                                    </TextBox.ToolTip>
                                                </local:IntegerTexBoxControl>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="2">
                                                <Label x:Name="LbMaxHeterozygousVariants" Content="Max Heterozygous Variants for Combinitorics:" Width="250" />
                                                <local:IntegerTexBoxControl x:Name="MaxHeterozygousVariantsTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <TextBox.ToolTip>
                                                        <TextBlock>
                                                    The maximum number of heterozygous variants for combinitorics.
                                                        </TextBlock>
                                                    </TextBox.ToolTip>
                                                </local:IntegerTexBoxControl>
                                            </StackPanel>
                                        </StackPanel>
                                    </GroupBox>
                                    <GroupBox Header="Fragment Ion Search Parameters">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="10,5,5,5">
                                                <CheckBox x:Name="AddCompIonCheckBox" Content="Generate Complementary Ions" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <CheckBox.ToolTip>
                                                        <TextBlock>
                                                    Generates experimental (not theoretical) complementary ions for each MS2.
                                                    <LineBreak />
                                                    Useful for localization of modifications.
                                                        </TextBlock>
                                                    </CheckBox.ToolTip>
                                                </CheckBox>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <Label x:Name="maxFragmentSize" Content="Max Fragment Mass (Da):" Width="146" />
                                                <local:DoubleTextBoxControl x:Name="MaxFragmentMassTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <TextBox.ToolTip>
                                                        <TextBlock>
                                                        Indexed Search Only: Limits the maximum MS2 fragment mass to avoid rare crashes.
                                                        <LineBreak />
                                                        A very high value (e.g. 30000) does not impact performance.
                                                        </TextBlock>
                                                    </TextBox.ToolTip>
                                                </local:DoubleTextBoxControl>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <Label Content="Max Threads:" Width="146" />
                                                <local:IntegerTexBoxControl x:Name="MaxThreadsTextBox" Width="45" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500">
                                                    <TextBox.ToolTip>
                                                        <TextBlock>
                                                        By default, MetaMorpheus uses this machine's number of threads minus one to maximize search speed while letting you use your computer for other purposes.
                                                        <LineBreak />
                                                        Enter a lower number to limit CPU usage.
                                                        </TextBlock>
                                                    </TextBox.ToolTip>
                                                </local:IntegerTexBoxControl>
                                            </StackPanel>
                                        </StackPanel>
                                    </GroupBox>

                                    <local:MassDifferenceAcceptorControl x:Name="MassDifferenceAcceptorControl" d:DataContext="{x:Static guiFunctions:MassDifferenceAcceptorSelectionModel.Instance}" />
                                </StackPanel>
                            </Expander>
                        </GroupBox>
                    </StackPanel>
                </Expander>
                <!-- END ADVANCED SETTINGS -->

            </DockPanel>
        </ScrollViewer>

        <StackPanel  Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1" Margin="5">
            <Button x:Name="cancelButton" Content="Cancel" Click="CancelButton_Click" />
            <Button x:Name="saveButton" Content="Save Fragment Discovery Task" Click="SaveButton_Click" FontSize="24" />
            <Button x:Name="setDefaultbutton" Click="SaveAsDefaultButton_Click" Width="100" >
                <TextBlock Text="Save As Default" TextWrapping="Wrap" TextAlignment="Center"/>
                <ToolTipService.ToolTip>
                    <ToolTip Content="Save these parameters as the default. MetaMorpheus can return to original defaults by deleting the folder 'DefaultParameters'" ToolTipService.ShowDuration="999999" ToolTipService.InitialShowDelay="500" />
                </ToolTipService.ToolTip>
            </Button>
        </StackPanel>

    </Grid>
</Window>
